name: AppCDS capability tests

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  focused:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven
      - name: Run AppCDS capability tests
        shell: bash
        run: |
          set +e
          mvn --batch-mode --no-transfer-progress \
            -pl preflight-core,preflight-synthetic-startup -am \
            -Dtest=AppCdsCapabilityDetectorTest,SyntheticAppCdsCrossProcessTest \
            -Dsurefire.failIfNoSpecifiedTests=false \
            test > appcds-capability-maven.log 2>&1
          status=$?
          cat appcds-capability-maven.log
          exit $status
      - name: Upload focused logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: appcds-capability-${{ matrix.os }}
          path: appcds-capability-maven.log
          if-no-files-found: error
